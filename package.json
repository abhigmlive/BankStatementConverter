{
  "name": "bank-statement-converter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Browser-only bank statement PDF to Excel/CSV converter (PDF.js + Tesseract.js + SheetJS), vendored locally. No server, no install, fully offline.",
  "scripts": {
    "test": "node --test tests/js/*.js",
    "test:browser": "node tests/browser/smoke.mjs",
    "serve": "python3 -m http.server 8080",
    "vendor": "bash scripts/vendor.sh"
  },
  "comment": "All deps are dev/build-time only — used to (re)generate assets/vendor and to run tests. The shipped app loads nothing from npm or any CDN at runtime.",
  "devDependencies": {
    "pdfjs-dist": "4.10.38",
    "tesseract.js": "5.1.1",
    "tesseract.js-core": "5.1.0",
    "xlsx": "0.18.5",
    "playwright-core": "1.49.1"
  }
}
